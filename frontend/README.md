# Web Interface Front End
These files should be copied to the root directory of the web server being hosted on the hyperloop pod.
To access the server, connect to the Pod's Wifi and go to 192.168.0.1 in your browser
