# install header only websocketpp library
# works on ubuntu. For installing you need admin access
git clone https://github.com/manchester-hyperloop/websocketpp
cd websocketapp
cmake .
sudo make install
cd ..
